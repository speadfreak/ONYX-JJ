/**
 * Site-wide animated film-grain overlay.
 * Keeps the obsidian blacks alive — pure CSS, GPU-cheap, pointer-transparent.
 */
export function GrainOverlay() {
  return <div aria-hidden className="jj-grain" />;
}
